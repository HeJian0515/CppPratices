#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, k;
    cin >> n >> k;
    vector<vector<int>> g(n-1, vector<int>(3));
    for (int i = 0; i < n-1; ++i) {
        cin >> g[i][0] >> g[i][1] >> g[i][2];
        
    }
    cout << 10;
}
